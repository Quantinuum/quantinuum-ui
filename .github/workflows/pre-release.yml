name: Pre Release library
on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to prerelease from"
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Ensure workflow is run from main
        if: ${{ github.ref_name != 'main' }}
        run: |
          echo "This workflow must be run with 'Use workflow from: main'."
          echo "Current workflow ref: ${{ github.ref_name }}"
          exit 1

      - name: Check out code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: 0

      - name: Set up node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm via corepack
        run: corepack enable pnpm

      - run: pnpm i --frozen-lockfile
      - run: pnpm run lint
      - run: pnpm run build

      - name: Fetch main branch for semantic-release
        run: git fetch origin main:main --tags

      - name: Force prerelease config
        env:
          BRANCH_NAME: ${{ github.event.inputs.branch }}
        run: |
          cat > .releaserc.json << EOF
          {
            "branches": [
              "main",
              { "name": "${BRANCH_NAME}", "prerelease": true }
            ]
          }
          EOF

      - run: pnpm exec semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
